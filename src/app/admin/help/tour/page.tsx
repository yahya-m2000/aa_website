import { TourRunner } from '@/features/admin-help/tour/tour-runner';

export const metadata = { title: 'Interactive Walkthrough — A&A Admin' };

export default function AdminHelpTourPage() {
  return <TourRunner />;
}
